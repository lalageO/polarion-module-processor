你现在接手 polarion-module-processor 项目。

当前项目已经能完成：
1. 通过 SVN 下载 Polarion 文档 module.xml；
2. 解析 XML；
3. 创建 Work Item；
4. 用真实 Work Item ID 替换 XML 中的 <p>；
5. 在 output/{jobId}/ 下生成 processed_module.xml。

现在需要完成最后一步：把处理后的 XML 自动提交回 SVN，让 Polarion 文档真正更新。

请按以下要求修改代码。

一、处理后文件名调整

当前输出文件名是：

processed_module.xml

请把正式流程中生成的处理后 XML 文件名改为：

module.xml

原因：Polarion 只识别文档目录下的 module.xml。

要求：
1. 正式 import 流程输出 output/{jobId}/module.xml。
2. original_module.xml、import_result.json、import_preview.csv 保持不变。
3. 旧 debug 接口如果还在用 processed_module.xml，可以不强制修改；正式流程必须输出 module.xml。
4. 响应里的 processedXmlFile / result files 也要指向 module.xml。

二、新增 SVN 自动提交能力

新增类：

SvnModuleCommitter

职责：

1. 每次任务在 output/{jobId}/ 下创建独立目录：
   svn-workspace/

2. 重新 checkout 模块目录：
   svn checkout --depth=empty <moduleDirUrl> svn-workspace

3. 在 svn-workspace 下拉取受版本控制的 module.xml：
   svn update module.xml

4. 用 output/{jobId}/module.xml 覆盖：
   svn-workspace/module.xml

5. 执行：
   svn status module.xml

6. 根据 status 判断：
   - 以 M 开头：执行 commit
   - 空输出：返回 NO_CHANGE
   - ? / C / ! 等：返回失败

7. 执行提交：
   svn commit -m "update module.xml" module.xml

8. 从输出中解析 revision：
   Committed revision 123456.

9. 返回提交结果。

三、SVN 命令实现要求

1. 使用 ProcessBuilder 参数数组。
2. 不要使用 cmd /c。
3. 不要使用 PowerShell。
4. 不要拼接 shell 字符串。
5. 工作目录通过 ProcessBuilder.directory(...) 设置。
6. 复用当前 ModuleXmlDownloader 里已有的 SVN 命令执行逻辑，或抽取公共 SvnCommandExecutor。
7. 日志里不要打印 SVN 密码。

四、配置

尽量复用当前已有的 polarion.svn 配置。

如果已有：
- executable
- username
- password
- checkout/update timeout
- moduleFileName
- base url / svn url 拼接逻辑

就直接复用。

只在必要时新增：

polarion.svn.commit-timeout-seconds=60
polarion.svn.default-commit-message=update module.xml
polarion.svn.cleanup-commit-workspace=false

不需要改 PolarionModuleImportRequest 增加一堆字段。

五、正式流程接入位置

在 PolarionModuleImportService 中，XML rewrite 完成并写出 output/{jobId}/module.xml 后，调用：

svnModuleCommitter.commit(...)

提交输入至少包含：
- jobId
- outputDir
- projectId
- moduleFolder
- moduleName
- processed module.xml 路径
- commit message，默认 "update module.xml"

提交成功后，把结果写入 import_result.json 和接口响应。

六、提交结果模型

新增一个简单结果对象：

SvnCommitResult

字段：

- success
- status
  - COMMITTED
  - NO_CHANGE
  - COMMIT_FAILED
- revision
- workspaceDir
- statusBeforeCommit
- statusAfterCommit
- errorMessage

响应中增加或复用字段：

- svnCommitStatus
- svnRevision
- svnErrorMessage

七、状态判断

svn status module.xml 的结果：

1. "M       module.xml"
   -> 执行 commit

2. 空字符串
   -> 不 commit，返回 success=true, status=NO_CHANGE

3. "?       module.xml"
   -> 失败，说明 module.xml 未受版本控制

4. "C       module.xml"
   -> 失败，存在冲突

5. "!       module.xml"
   -> 失败，working copy 异常

commit 成功判断：
1. 进程 exitCode == 0
2. 输出包含 "Committed revision"

revision 正则：
Committed revision (\\d+)

八、接口响应行为

如果 SVN 提交成功：

success=true
status=COMMITTED
svnCommitStatus=COMMITTED
svnRevision=实际 revision

如果没有变化：

success=true
svnCommitStatus=NO_CHANGE

如果提交失败：

success=false
svnCommitStatus=COMMIT_FAILED
svnErrorMessage=失败原因

注意：
即使 commit 失败，也不要删除 output/{jobId}/module.xml、import_result.json、import_preview.csv，方便排查。

九、测试要求

请新增或更新测试：

1. processedXmlFileNameShouldBeModuleXml
   - 正式流程输出文件名应是 module.xml

2. svnStatusModifiedShouldCommit
   - status 返回 M
   - commit 输出 Committed revision 123456.
   - 结果为 COMMITTED，revision=123456

3. svnStatusCleanShouldReturnNoChange
   - status 返回空
   - 不执行 commit
   - 结果为 NO_CHANGE

4. svnStatusQuestionMarkShouldFail
   - status 返回 ?
   - 结果为 COMMIT_FAILED

5. importServiceShouldCallCommitterAfterRewrite
   - rewrite 生成 module.xml 后调用 SvnModuleCommitter

十、不要做的事

1. 不要大改请求 DTO。
2. 不要新增复杂的 commitToSvn / allowCommitWithErrors 参数。
3. 不要改 LLM 逻辑。
4. 不要改 Work Item 创建逻辑。
5. 不要引入数据库。
6. 不要删除旧 debug 接口。
7. 不要提交 processed_module.xml。
8. 不要复用第一次下载后已经被清理的 SVN 临时目录。
9. 不要自动处理 SVN 冲突。