你现在接手项目 polarion-module-processor。

当前项目已经实现：
1. 通过 SVN/HTTP 下载 module.xml；
2. 提取 homePageContent CDATA；
3. 扫描并识别候选 item；
4. 生成 title/description；
5. 输出 import_result.json、import_preview.csv；
6. 目前 HttpPolarionWorkItemCreator 还是占位实现，返回 POLARION_API_NOT_IMPLEMENTED。

现在请你完成“调用公司内部封装好的 Polarion createItem 接口”的适配工作。

## 一、目标

把当前占位的 HttpPolarionWorkItemCreator 改成真实 HTTP 调用。

公司内部接口：

POST http://10.179.60.154:30000/workitem/ws/create
Content-Type: application/json

Dify 中环境变量名：

FDP_POLARION_URL = http://10.179.60.154:30000/workitem/ws/create

成功响应示例：

{
  "code": "0",
  "data": "FDP-7018",
  "extension": {},
  "success": true,
  "msg": "请求成功"
}

其中 response.data 就是真实 Work Item ID，后续用于生成：

<div id="polarion_wiki macro name=module-workitem;params=id=FDP-7018"></div>

严禁在正式流程中生成 mock ID，例如 FDP-000001、MOCK-000001。

## 二、请求体字段

Dify 中的请求体参考：

{
  "polarionId": "FDP_Demo",
  "type": "stakeholderrequirement",
  "title": "11 DCAS 应具备评估驾驶员持续参与能力",
  "authorId": "yiming.yuan",
  "isNewPdp": false,
  "onlyCreate": true,
  "description": "【条款 11】原文：DCAS shall have means to evaluate continuous driver involvement in and supervision of the vehicle operation。\n\nDCAS 应具备评估驾驶员持续参与和监督车辆运行的手段。",
  "customFields": [
    {
      "id": "verificationcriteria",
      "multi": false,
      "type": "text/html",
      "value": "测试验证：通过模拟不同驾驶场景，确认 DCAS 能够准确识别驾驶员是否持续参与驾驶任务。"
    },
    {
      "id": "requirementsource",
      "multi": false,
      "type": "EnumOptionId",
      "value": "Regulation"
    }
  ]
}

请注意：

1. 当前 HTTP 请求字段使用 description，不使用 cdescription。
2. 只适配必要字段，不要把 WorkItemWsDto 中所有字段都实现。
3. onlyCreate 固定为 true。
4. isNewPdp 固定为 false。
5. type 默认 stakeholderrequirement，注意全小写。
6. authorId 从正式导入请求或配置中取，不要写死在代码里。
7. customFields 要支持默认配置和请求参数扩展。

## 三、需要新增/调整的配置

在 application.yml 中补充或调整：

polarion:
  work-item-api:
    enabled: true
    create-url: "http://10.179.60.154:30000/workitem/ws/create"
    default-polarion-id: "FDP_Demo"
    default-type: "stakeholderrequirement"
    default-author-id: "yiming.yuan"
    connect-timeout-ms: 5000
    read-timeout-ms: 30000
    default-custom-fields:
      - id: "requirementsource"
        multi: false
        type: "EnumOptionId"
        value: "Regulation"

可选字段：
- verificationcriteria 后续可能由 LLM 生成，当前可以先为空、规则生成，或由请求传入。
- reqType=functional 之前出现过，但当前 Dify 截图主流程里未必必传，不要硬编码为必填。可作为 default-custom-fields 的配置项支持。

如果 enabled=false 或 create-url 为空：
- 正式 dryRun=false 时不要生成假 ID；
- item 标记 CREATE_FAILED；
- errorMessage 写 POLARION_API_NOT_CONFIGURED。

## 四、请求 DTO 设计

请不要直接把内部通用 WorkItemCreateRequest 当作最终 HTTP 请求体。

保留当前内部模型：

WorkItemCreateRequest
- projectId
- type
- title
- description
- authorId
- fields 或 customFields

新增公司接口专用 DTO：

WorkItemCreateApiRequest

字段：

public class WorkItemCreateApiRequest {
    private String polarionId;
    private String type;
    private String title;
    private String authorId;
    private Boolean isNewPdp = Boolean.FALSE;
    private Boolean onlyCreate = Boolean.TRUE;
    private String description;
    private List<PolarionCustomFieldRequest> customFields;
}

新增：

public class PolarionCustomFieldRequest {
    private String id;
    private Boolean multi;
    private String type;
    private Object value;
}

新增响应 DTO：

public class WorkItemCreateApiResponse {
    private String code;
    private String data;
    private Map<String, Object> extension;
    private Boolean success;
    private String msg;
}

映射规则：
- WorkItemCreateRequest.projectId -> polarionId
- WorkItemCreateRequest.type -> type
- WorkItemCreateRequest.title -> title
- WorkItemCreateRequest.description -> description
- WorkItemCreateRequest.authorId -> authorId
- customFields -> customFields
- isNewPdp -> false
- onlyCreate -> true

## 五、Builder 设计

新增 WorkItemCreateApiRequestBuilder。

职责：
1. 把内部 WorkItemCreateRequest 转成公司接口请求体；
2. 合并 customFields；
3. 填充默认字段；
4. 不负责发 HTTP 请求。

合并 customFields 的规则：

1. 配置中的 default-custom-fields 先加入；
2. 请求中的 defaultFields/customFields 后加入；
3. item 动态字段最后加入；
4. 如果 id 相同，后加入的覆盖先加入的；
5. 最终输出 List<PolarionCustomFieldRequest>。

注意：
- requirementsource=Regulation 可以来自配置。
- verificationcriteria 如果 item 中没有，就不要强行塞空字符串，除非配置明确要求。
- reqType=functional 不要硬编码，只允许通过配置或请求传入。

## 六、HttpPolarionWorkItemCreator 实现要求

当前类：

HttpPolarionWorkItemCreator

请把占位实现改成真实 HTTP 调用。

建议用 RestTemplate 或当前项目已有 HTTP 客户端。若项目还没有 RestTemplate Bean，请新增配置类提供一个带超时的 RestTemplate。

伪代码：

public WorkItemCreateResult createOne(WorkItemCreateRequest request) {
    if (!api.enabled || createUrl blank) {
        return failure("POLARION_API_NOT_CONFIGURED", ...);
    }

    WorkItemCreateApiRequest apiRequest = builder.build(request);

    POST createUrl
    Header Content-Type = application/json
    Body = apiRequest

    if http status not 2xx:
        return failure("POLARION_API_HTTP_ERROR", ...);

    if body == null:
        return failure("POLARION_API_EMPTY_RESPONSE", ...);

    if body.success == true && body.code == "0" && body.data not blank:
        return success(body.data);

    return failure(body.code, body.msg);
}

异常处理：
- 捕获 RestClientException、SocketTimeoutException 等；
- 返回 WorkItemCreateResult.failure("POLARION_API_EXCEPTION", e.getMessage())；
- 不抛出导致整个任务中断；
- 不生成假 ID。

日志要求：
- 记录请求 item 的 seq/title，不要完整打印超长 description；
- 记录返回的 workItemId；
- 失败时记录 code/msg。

## 七、正式导入流程中的调用要求

当前 PolarionModuleImportService 里已经有 createWorkItems 方法。请检查并调整：

1. 对每个 candidate=true 的 item 串行处理。
2. 先生成 title 和 description。
3. 构造 WorkItemCreateRequest。
4. 调用 PolarionWorkItemCreator.createOne。
5. 成功后：
   - item.workItemId = response.data；
   - item.status = CREATED；
   - 立即原子写 import_result.json；
   - 更新 CSV。
6. 失败后：
   - item.status = CREATE_FAILED；
   - item.errorMessage = errorCode + ": " + errorMessage；
   - 继续处理下一个 item。
7. 所有可创建 item 处理完后，只对 status=CREATED 且 workItemId 非空的 item 做 XML 替换。
8. 替换成功后 item.status = REPLACED。
9. 替换失败后 item.status = REPLACE_FAILED。
10. import_result.json 中必须保留 workItemId。

不要在 createWorkItems 中做并发，第二版先串行。

## 八、PolarionModuleImportRequest 字段调整

请检查当前 PolarionModuleImportRequest，补充以下字段：

- authorId
- defaultFields 或 customFields
- dryRun
- workItemType
- requireKeyword

建议：

public class PolarionModuleImportRequest {
    private String projectId;
    private String moduleFolder;
    private String moduleName;
    private String workItemType;
    private String authorId;
    private Boolean dryRun;
    private Boolean requireKeyword;
    private Map<String, Object> defaultFields;
    private List<PolarionCustomFieldRequest> customFields;
}

说明：
- authorId 不传时使用 polarion.work-item-api.default-author-id。
- workItemType 不传时使用 polarion.work-item-api.default-type。
- projectId 不传时使用配置 default-polarion-id。
- defaultFields 用于简单键值扩展。
- customFields 用于精确传入 id/multi/type/value 结构。

如果同时存在 defaultFields 和 customFields：
- defaultFields 可以转成 customFields，但只有在能确定 type 时才转；
- 更推荐 customFields 直接承接复杂字段；
- 对 requirementsource 这类字段，优先通过 default-custom-fields 配置。

## 九、JSON 输出调整

import_result.json 中每个 item 保留：

- seq
- itemKey
- startParagraphId
- endParagraphId
- outlineNo
- title
- description
- candidate
- skipReason
- workItemCreateFields
- customFields
- workItemId
- status
- errorMessage

注意：
- workItemId 创建成功后必须立即写入 JSON。
- 不要输出 replacementHtml，div 可由 workItemId 动态生成。
- 不要输出 mockIdPrefix。
- 不要输出 fake ID。
- 可以把 customFields 写入 JSON，便于排查实际创建参数。

## 十、CSV 输出调整

import_preview.csv 字段建议：

seq
outlineNo
startParagraphId
endParagraphId
candidate
skipReason
title
workItemId
status
errorMessage
description

如果方便，可增加 customFieldsSummary，但不要把完整 JSON 展开成很多列。

## 十一、Description 构造

当前可以使用 item.description 作为 description。

不要先塞复杂 HTML。后续确认 description 支持 HTML 后再调整。

verificationcriteria 暂时不依赖 LLM。可选：
- 如果请求或配置里传了 verificationcriteria，就带上；
- 未传就不带；
- 不要阻塞 Work Item 创建。

## 十二、验收标准

完成后应满足：

1. dryRun=true：
   - 只下载/解析/识别；
   - 不调用 /workitem/ws/create；
   - 不生成 Work Item ID；
   - 不改写 XML。

2. dryRun=false 且 API 配置正确：
   - 串行调用 http://10.179.60.154:30000/workitem/ws/create；
   - 请求体字段与 Dify 示例一致；
   - 成功时从 response.data 取 FDP-xxxx；
   - 写入 import_result.json；
   - 用 FDP-xxxx 替换 XML 生成 div；
   - 输出 processed_module.xml。

3. API 失败：
   - 当前 item 标记 CREATE_FAILED；
   - 继续下一个 item；
   - 不生成任何 mock ID。

4. 配置缺失：
   - 返回 POLARION_API_NOT_CONFIGURED；
   - 不 fallback 到 mock。

5. 测试通过：
   - mvn test。

## 十三、建议新增测试

1. buildApiRequestMatchesDifyPayload
   - 输入 WorkItemCreateRequest；
   - 断言输出 JSON 包含：
     polarionId/type/title/authorId/isNewPdp=false/onlyCreate=true/description/customFields。

2. createOneSuccessReturnsDataAsWorkItemId
   - mock HTTP 返回：
     {"code":"0","data":"FDP-7018","success":true,"msg":"请求成功"}
   - 断言 WorkItemCreateResult.success=true，workItemId=FDP-7018。

3. createOneFailureDoesNotGenerateFakeId
   - mock HTTP 返回 success=false；
   - 断言 workItemId=null；
   - 断言 errorMessage 不为空。

4. importServiceWritesWorkItemIdAfterCreate
   - fake creator 返回 FDP-7018；
   - 断言 import_result.json 中对应 item 有 workItemId=FDP-7018。

5. dryRunDoesNotCallCreator
   - dryRun=true；
   - 断言 creator 未被调用。

## 十四、不要做的事情

1. 不要实现 LLM 标题生成。
2. 不要做并发创建。
3. 不要引入数据库。
4. 不要删除旧 debug 接口。
5. 不要在正式接口中支持 mockIdPrefix。
6. 不要在正式接口中生成 FDP-000001。
7. 不要把 WorkItemWsDto 所有字段都搬进本地项目。
8. 不要让一次 item 创建失败中断整个任务。