你现在接手一个 Spring Boot 项目：polarion-module-processor。

当前代码已经实现了第一版本地调试能力：
- /api/module/process 接收本地上传的 module.xml；
- 解析 homePageContent CDATA；
- 扫描 <p>；
- 分组识别候选 item；
- 生成 JSON/CSV；
- replaceMode=MOCK 时用 mockIdPrefix 生成 FDP-000001 / MOCK-000001 之类假 ID；
- 把 <p> 替换成 module-workitem div。

现在需要进行第二版重构。请先阅读当前代码，重点关注：
- ModuleProcessController
- ModuleProcessService
- ModuleProcessorProperties
- ModuleXmlExtractor
- ParagraphScanner
- NumberedItemGrouper
- ParagraphCandidateSelector
- RuleBasedTitleGenerator
- ModuleXmlRewriter
- MockWorkItemIdProvider
- WorkItemIdProvider
- ImportResultWriter
- ImportPreviewCsvWriter
- ImportItemResult
- ImportJobResult
- ModuleProcessRequest
- ModuleProcessResponse
- ReplaceMode
- TitleMode
- application.yml
- 现有测试类

## 一、重构目标

保留第一版调试接口，但新增一个正式业务链路。

正式链路目标：

1. 后端根据 projectId、moduleFolder、moduleName 拼接 Polarion module.xml 下载地址；
2. 通过 HTTP 请求下载 module.xml 到本地任务目录；
3. 读取并解析 module.xml；
4. 提取 homePageContent CDATA；
5. 识别其中应转换为 Work Item 的 item；
6. 生成 title 和 description；
7. 构造 Polarion Work Item 创建请求对象；
8. 调用 Polarion API 创建真实 Work Item；
9. 获取 Polarion 返回的真实 workItemId，例如 FDP-7016；
10. 立即把 workItemId 写入 import_result.json；
11. 用真实 workItemId 生成 module-workitem div；
12. 替换 XML 中对应 <p> 或 <p> 分组；
13. 输出 processed_module.xml；
14. 输出收缩后的 import_result.json 和 import_preview.csv。

注意：
- 正式流程中严禁生成 FDP-000001、MOCK-000001 这类假 ID。
- 正式流程中严禁使用 mockIdPrefix。
- 正式流程中严禁 replaceMode=MOCK。
- 旧的 /api/module/process 可以保留给本地调试和 mock XML 替换，但必须标记为 Debug / Deprecated。
- 新正式接口默认 dryRun=false。
- dryRun 只作为开发调试选项：只下载、解析、识别、输出 JSON/CSV，不创建 Work Item，不改写 XML。

## 二、接口调整

### 1. 保留旧接口

保留现有：

POST /api/module/process

定位：
- 本地上传 module.xml；
- 验证 XML 解析、候选识别、mock 替换；
- 仅用于开发调试。

处理要求：
- Controller 或响应 message 中明确说明该接口是 debug/deprecated。
- 旧接口可以继续支持 replaceMode=MOCK 和 mockIdPrefix。
- 旧接口中的 mock 逻辑不得被新正式流程复用。
- 不要删除旧接口现有核心功能，避免破坏已有调试能力。

### 2. 新增正式接口

新增 Controller，例如：

PolarionModuleImportController

新增接口：

POST /api/polarion/module/import
Content-Type: application/json

请求 DTO 建议命名：

PolarionModuleImportRequest

字段：

```java
public class PolarionModuleImportRequest {
    private String projectId;
    private String moduleFolder;
    private String moduleName;
    private String workItemType;

    // 默认 false。true 时只下载、解析、识别、生成 JSON/CSV。
    private Boolean dryRun;

    // 是否要求 shall/may/should 等关键词，默认 false 或从配置读取。
    private Boolean requireKeyword;

    // 用于动态承接后续 Polarion API 需要的扩展字段。
    // 例如 requirementsource/status/ASIL/CAL/targetVersion/assignee/reference 等。
    private Map<String, Object> defaultFields;
}

```

响应 DTO 建议命名：

PolarionModuleImportResponse

```java
public class PolarionModuleImportResponse {
    private Boolean success;
    private String jobId;
    private String projectId;
    private String moduleFolder;
    private String moduleName;
    private Boolean dryRun;
    private String status;

    private Integer paragraphCount;
    private Integer itemCount;
    private Integer candidateCount;
    private Integer createdCount;
    private Integer replacedCount;
    private Integer skippedCount;
    private Integer failedCount;

    private String outputDir;
    private String originalXmlFile;
    private String processedXmlFile;
    private String resultJsonFile;
    private String previewCsvFile;

    private String message;
}
```

## 三、正式业务全流程

新增服务：

PolarionModuleImportService

正式流程按以下步骤编排：

1.  接收 PolarionModuleImportRequest。
2.  设置默认值：
    -   dryRun 默认 false；
    -   requireKeyword 默认从配置读取；
    -   workItemType 可从请求读取，也可从配置读取默认值。
3.  创建 jobId，例如：
    -   R171e2_20260527_103000
4.  创建输出目录：
    -   output/{jobId}/
5.  调用 ModuleXmlDownloader 下载 module.xml。
6.  保存原始文件：
    -   original_module.xml
7.  校验下载内容：
    -   必须包含 <module>
    -   必须包含 <field id="homePageContent"
8.  调用 ModuleXmlExtractor 提取 CDATA。
9.  调用 ParagraphScanner 扫描 <p>。
10.  调用 NumberedItemGrouper 或现有分组逻辑生成 item。
11.  调用 ParagraphCandidateSelector 判断 candidate 和 skipReason。
12.  调用 TitleGenerator 生成 title。
     -   第二版暂时继续 RuleBasedTitleGenerator；
     -   不接 LLM；
     -   不保留复杂的 generatedTitle/finalTitle/llmTitle 多字段。
13.  构造 description。
     -   默认可以用 sourceText。
     -   后续确认 Polarion Description 支持 HTML 后再扩展。
14.  构造 import_result.json 初始内容，状态为 ITEMS_READY。
15.  写入 import_result.json。
16.  写入 import_preview.csv。
17.  如果 dryRun=true，直接返回，不调用 Polarion API，不输出 processed_module.xml。
18.  如果 dryRun=false：
     -   遍历 candidate=true 的 item；
     -   构造 WorkItemCreateRequest；
     -   调用 PolarionWorkItemCreator.createOne；
     -   成功后获取真实 workItemId；
     -   立即更新该 item 的 workItemId 和 status=CREATED；
     -   立即原子写回 import_result.json；
     -   失败时 status=CREATE_FAILED，记录 errorMessage，继续下一条。
19.  对所有 status=CREATED 且有 workItemId 的 item：
     -   调用 ModuleWorkItemMacroRenderer 生成 div；
     -   调用 ModuleXmlRewriter 替换对应 <p> 或 <p> 分组；
     -   成功后 status=REPLACED；
     -   失败后 status=REPLACE_FAILED，记录 errorMessage。
20.  输出 processed_module.xml。
21.  更新最终 import_result.json。
22.  更新最终 import_preview.csv。
23.  返回响应。

## 四、HTTP 下载 module.xml

新增类：

ModuleXmlDownloader

职责：

-   根据 projectId、moduleFolder、moduleName 拼接 module.xml 下载路径；
-   通过 HTTP 请求下载内容；
-   返回字符串。

配置建议：

```
polarion:
  base-url: "http://alm.freetech.com"
  module-file-name: "module.xml"
  auth:
    type: "NONE" # NONE / COOKIE / BASIC / BEARER
    cookie: ""
    username: ""
    password: ""
    token: ""
```

拼接规则：

原始路径：

{projectId}/modules/{moduleFolder}/{moduleName}/module.xml

示例：

FDP_Demo/modules/10 Stakeholder Requirement/R171e2/module.xml

URL 编码后拼接：

{baseUrl}/polarion/svnwebclient/fileContent.jsp?url={encodedPath}

编码要求：

-   / 编码为 %2F；
-   空格编码为 %20；
-   Java 中可使用 URLEncoder.encode(rawPath, "UTF-8").replace("+", "%20")。

认证处理：

-   第一版先支持 NONE 和 COOKIE；
-   BASIC / BEARER 可预留；
-   如果返回内容是登录页或非 module.xml，要返回明确错误。
-   不要把认证逻辑写死在 Service 中，放在 Downloader 内部或独立 HttpClient 配置中。

## 五、Polarion API 创建 Work Item

当前暂时还没有同事提供具体 API 参数和地址。请先设计可扩展接口，避免继续使用 mock ID 误导正式流程。

新增接口：

```
public interface PolarionWorkItemCreator {
    WorkItemCreateResult createOne(WorkItemCreateRequest request);
}
```

新增 DTO：

```
public class WorkItemCreateRequest {
    private String projectId;
    private String type;
    private String title;
    private String description;

    // 动态扩展字段，承接 requirementsource/status/ASIL/CAL 等。
    private Map<String, Object> fields;
}
public class WorkItemCreateResult {
    private Boolean success;
    private String workItemId;
    private String errorCode;
    private String errorMessage;
}
```

新增实现类：

1.  HttpPolarionWorkItemCreator
    -   预留真实 HTTP 调用位置；
    -   API 地址、认证、请求体结构从配置读取；
    -   如果当前配置不完整，返回 success=false，errorCode=POLARION_API_NOT_CONFIGURED；
    -   不要生成假 ID。
2.  可以保留一个测试用 FakePolarionWorkItemCreator，但只能在 test profile 或测试代码中使用。
    -   不要在正式 profile 下生成 FDP-000001。
    -   不要用 mockIdPrefix。

重要要求：

-   正式主流程中，Work Item ID 只能来自 PolarionWorkItemCreator 的返回结果。
-   任何本地生成的 ID 都不能进入新正式接口。
-   创建成功后必须马上写入 import_result.json。

## 六、正式流程中的 JSON 重新设计

当前 ImportJobResult / ImportItemResult 字段过多，且包含很多过渡字段。请为新正式流程新增或重构模型，建议命名：

PolarionImportJobResult
 PolarionImportItemResult

正式流程输出文件名：

import_result.json

结构：

```
{
  "jobId": "R171e2_20260527_103000",
  "projectId": "FDP_Demo",
  "moduleFolder": "10 Stakeholder Requirement",
  "moduleName": "R171e2",
  "workItemType": "stakeholderRequirement",
  "dryRun": false,
  "status": "COMPLETED",
  "files": {
    "originalXml": "original_module.xml",
    "processedXml": "processed_module.xml",
    "csv": "import_preview.csv"
  },
  "summary": {
    "paragraphCount": 1305,
    "itemCount": 120,
    "candidateCount": 118,
    "createdCount": 118,
    "replacedCount": 118,
    "skippedCount": 2,
    "failedCount": 0
  },
  "items": [
    {
      "seq": 1,
      "itemKey": "polarion_148",
      "startParagraphId": "polarion_148",
      "endParagraphId": "polarion_148",
      "outlineNo": "5.1.1.",
      "title": "5.1.1 The system shall be designed to ensure the driver remains engaged",
      "description": "5.1.1. The system shall be designed to ensure the driver remains engaged with the driving task...",
      "candidate": true,
      "skipReason": null,
      "workItemCreateFields": {
        "type": "stakeholderRequirement",
        "project": "FDP_Demo",
        "requirementsource": "Regulation",
        "status": "open"
      },
      "workItemId": "FDP-7016",
      "status": "REPLACED",
      "errorMessage": null
    }
  ]
}
```

### JSON 字段保留要求

Job 级别保留：

-   jobId
-   projectId
-   moduleFolder
-   moduleName
-   workItemType
-   dryRun
-   status
-   files
-   summary
-   items

Item 级别保留：

-   seq
-   itemKey
-   startParagraphId
-   endParagraphId
-   outlineNo
-   title
-   description
-   candidate
-   skipReason
-   workItemCreateFields
-   workItemId
-   status
-   errorMessage

### JSON 中删除或避免输出

正式流程 JSON 不要输出以下字段：

-   replacementHtml
-   sourceStartIndex
-   sourceEndIndex
-   sourceOuterHtmlHash
-   paragraphKey
-   phase
-   llmTitle
-   generatedTitle
-   finalTitle
-   每条 item 的 createdAt / updatedAt
-   mockIdPrefix
-   replaceMode
-   mock Work Item ID

说明：

-   replacementHtml 可根据 workItemId 随时生成。
-   sourceStartIndex/sourceEndIndex 属于内部替换细节。
-   第二版 title 字段统一表示将传给 Polarion 的标题。

## 七、正式流程中的 CSV 重新设计

正式流程输出文件名：

import_preview.csv

CSV 字段固定为：

```
seq
outlineNo
startParagraphId
endParagraphId
candidate
skipReason
title
workItemId
status
errorMessage
description
```

要求：

-   CSV 面向人工检查。
-   字段越少越清晰。
-   不输出 hash、替换 HTML、内部 index。
-   继续支持 UTF-8，必要时保留 BOM 以方便 Excel 打开。

## 八、状态枚举调整

现有 ItemStatus 可以扩展或新增正式流程状态：

建议保留：

-   READY：已识别，等待创建 Work Item；
-   SKIPPED：跳过；
-   CREATING：正在创建 Work Item；
-   CREATED：Work Item 创建成功，已有真实 workItemId；
-   CREATE_FAILED：创建 Work Item 失败；
-   REPLACED：XML 替换成功；
-   REPLACE_FAILED：XML 替换失败；
-   FAILED：其他失败。

Job 状态建议：

-   ITEMS_READY
-   CREATING_WORK_ITEMS
-   REWRITING_XML
-   COMPLETED
-   COMPLETED_WITH_ERRORS
-   FAILED

## 九、XML 替换调整

新增类：

ModuleWorkItemMacroRenderer

职责：

-   统一根据真实 workItemId 生成 Polarion 宏 div。

输出格式：

```
<div id="polarion_wiki macro name=module-workitem;params=id={workItemId}"></div>
```

要求：

-   不要在多个地方散落字符串拼接。
-   ModuleXmlRewriter 不负责判断 ID 来源，只负责用已生成的 replacementHtml 替换目标段落。
-   正式流程中，只有 status=CREATED 且 workItemId 非空的 item 可以进入替换。

ModuleXmlRewriter 保留现有局部替换策略：

-   不使用 Jsoup 序列化整个 HTML；
-   只替换 CDATA 中目标 <p> 或目标段落组；
-   不改动非目标内容；
-   不改动外层 XML。

## 十、旧 mock 逻辑处理

请按以下方式处理当前 mock 相关代码：

1.  MockWorkItemIdProvider
    -   保留给旧 /api/module/process 或测试；
    -   不得被 /api/polarion/module/import 使用。
2.  WorkItemIdProvider
    -   如果只服务 mock，可保留在 legacy/debug 路径；
    -   正式流程使用 PolarionWorkItemCreator。
3.  ReplaceMode
    -   保留给旧接口；
    -   新接口不得暴露 replaceMode。
    -   新接口中不存在 MOCK 模式。
4.  ModuleProcessService
    -   保留旧逻辑；
    -   可标记 Legacy/Debug；
    -   不要在新正式服务中复用 assignMockWorkItemIds 或 mock replacement 流程。
5.  ModuleProcessController
    -   加 @Deprecated 或注释；
    -   响应 message 中说明该接口仅用于本地调试。

## 十一、配置调整

application.yml 建议新增：

```yaml
module-processor:
  output-dir: output
  default-module-name: unknown-module
  default-min-outline-depth: 2
  default-require-keyword: false
  default-include-sub-items: false
  level-two-min-text-length: 80
  title-max-length: 80
  csv-with-bom: true

polarion:
  base-url: "http://alm.freetech.com"
  module-file-name: "module.xml"
  default-project-id: "FDP_Demo"
  default-module-folder: "10 Stakeholder Requirement"
  default-work-item-type: "stakeholderRequirement"
  auth:
    type: "NONE"
    cookie: ""
    username: ""
    password: ""
    token: ""
  work-item-api:
    enabled: false
    create-url: ""
    auth-type: "NONE"
    token: ""
```

说明：

-   work-item-api.enabled=false 时，如果调用正式接口 dryRun=false，应返回明确错误 POLARION_API_NOT_CONFIGURED，或者在创建阶段把 item 标记为 CREATE_FAILED。
-   不允许 fallback 到 mock ID。
-   default-require-keyword 建议默认 false，避免遗漏法规条款。

## 十二、并发和网络 IO

第二版先使用顺序创建：

-   遍历 candidate item；
-   一个 item 一个 HTTP 请求；
-   成功一个，马上写 JSON；
-   失败一个，记录错误，继续下一条。

不要一开始做高并发。

可以预留配置，但不必实现：

```yaml
polarion:
  work-item-api:
    create-mode: SEQUENTIAL
    batch-size: 20
    max-concurrency: 3
    retry-times: 2
```

如果要预留接口，可以在 PolarionWorkItemCreator 中提供默认 batch 方法：

```java
default List<WorkItemCreateResult> createBatch(List<WorkItemCreateRequest> requests) {
    List<WorkItemCreateResult> results = new ArrayList<>();
    for (WorkItemCreateRequest request : requests) {
        results.add(createOne(request));
    }
    return results;
}
```

但正式流程第二版先调用 createOne。

## 十三、JSON 原子写

因为 Work Item 创建成功后是外部副作用，必须及时落盘。

要求：

-   每个 item 创建成功后，立即更新 import_result.json。
-   写 JSON 使用临时文件再 rename：
    -   import_result.json.tmp
    -   import_result.json
-   避免程序中断导致 JSON 文件损坏。
-   程序中断后，至少能从 JSON 看到哪些 item 已拿到真实 workItemId。

## 十四、测试要求

请更新或新增测试。

保留旧测试：

-   ModuleXmlExtractorTest
-   ParagraphCandidateSelectorTest
-   旧 ModuleProcessServiceTest 可以适当调整，但不要破坏旧调试接口能力。

新增测试：

1.  downloaderBuildsEncodedModuleUrl
    -   输入 projectId/moduleFolder/moduleName；
    -   断言 URL 中路径被正确编码，空格为 %20，/ 为 %2F。
2.  polarionImportDryRunProducesJsonAndCsv
    -   模拟下载 module.xml；
    -   dryRun=true；
    -   断言输出 original_module.xml、import_result.json、import_preview.csv；
    -   断言不创建 Work Item；
    -   断言不输出 processed_module.xml 或 processedXmlFile=null。
3.  polarionImportDoesNotGenerateMockIds
    -   调用新正式接口或 Service；
    -   断言 JSON/CSV 中不存在 MOCK-000001、FDP-000001 等假 ID。
4.  polarionImportCreatesWorkItemsSequentially
    -   使用 fake/test PolarionWorkItemCreator 返回 FDP-7016、FDP-7017；
    -   dryRun=false；
    -   断言 item 中写入真实 workItemId；
    -   断言 processed_module.xml 中包含 params=id=FDP-7016。
5.  createFailureDoesNotStopWholeJob
    -   fake creator 某个 item 返回失败；
    -   断言该 item 为 CREATE_FAILED；
    -   后续 item 继续创建。
6.  workItemIdWrittenBeforeRewrite
    -   模拟创建成功后替换失败；
    -   断言 JSON 中仍保留 workItemId；
    -   status 为 REPLACE_FAILED。

## 十五、验收命令示例

旧调试接口继续支持：

```
curl.exe -s -X POST "http://localhost:8080/api/module/process" ^
  -F "file=@src/main/resources/module.xml" ^
  -F "moduleName=R171e2" ^
  -F "dryRun=false" ^
  -F "replaceMode=MOCK" ^
  -F "mockIdPrefix=FDP" ^
  -F "requireKeyword=false"
```

新正式接口示例：

```
curl.exe -s -X POST "http://localhost:8080/api/polarion/module/import" ^
  -H "Content-Type: application/json" ^
  -d "{\"projectId\":\"FDP_Demo\",\"moduleFolder\":\"10 Stakeholder Requirement\",\"moduleName\":\"R171e2\",\"workItemType\":\"stakeholderRequirement\",\"dryRun\":true,\"requireKeyword\":false,\"defaultFields\":{\"requirementsource\":\"Regulation\",\"status\":\"open\"}}"
```

dryRun=true 预期：

-   下载 module.xml；
-   输出 original_module.xml；
-   输出 import_result.json；
-   输出 import_preview.csv；
-   不创建 Work Item；
-   不输出 mock ID；
-   不改写 XML。

dryRun=false 预期：

-   若 Polarion Work Item API 未配置，返回明确错误或 item 标记 CREATE_FAILED；
-   绝不生成假 ID；
-   一旦真实 API 配置完成，使用 API 返回的 FDP-xxxx 生成 module-workitem div；
-   输出 processed_module.xml。

## 十六、最终交付要求

请完成以下交付：

1.  保留旧 debug 接口并标记 deprecated。
2.  新增正式业务接口 /api/polarion/module/import。
3.  新增 ModuleXmlDownloader。
4.  新增 PolarionWorkItemCreator、WorkItemCreateRequest、WorkItemCreateResult。
5.  新增 ModuleWorkItemMacroRenderer。
6.  新增或重构正式流程的 JSON/CSV 模型。
7.  正式流程 JSON/CSV 按本提示词字段收缩。
8.  正式流程中删除 mockIdPrefix、replaceMode=MOCK、假 ID 生成。
9.  Work Item 创建成功后立即原子写 JSON。
10.  单元测试更新并通过 mvn test。
11.  更新 README 或 AGENTS.md，明确：
     -   /api/module/process 是本地调试接口；
     -   /api/polarion/module/import 是正式业务接口；
     -   正式接口从 HTTP 下载 module.xml；
     -   正式接口只接受 Polarion API 返回的真实 Work Item ID；
     -   当前 LLM 未接入，标题暂用规则生成；
     -   当前 Polarion Work Item API 参数未知，使用 WorkItemCreateRequest.fields 动态扩展。
